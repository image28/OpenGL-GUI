/*int iconbox(struct container screen, struct color colour, SDL_Rect area, struct window list[255], int scrollbar, int frame, int update)
{
	int d=0;

	if ( frame )
		DrawFrame(&screen,colour,);
	
	for(d=0; d < windowcount; d++)
	{
		if ( list[d].iconified )
		{
			iconified[iconifiedcount]=d;
			iconifiedcount++;
		}
	}

	iconsvisible=area.x/(iconsize+space);

	for(d=scrollbar; d < scrollbar+iconsvisible; d++)
	{
		// Draw window icon d
	}

	if ( update )

}*/